using Gtk 4.0;
using Adw 1;

template $CheckLoadingRow: Adw.ActionRow {
  selectable: false;
  activatable-widget: check;

  [prefix]
  $LoadingBin bin {
    child: Gtk.CheckButton check {
      valign: center;
    };
  }
}
